
{% extends "base.html.twig" %}

{% block title %}Ajouter{% endblock %}

{% block body %}
    <h1 class="text-center my-4">Ajouter un article</h1>

    {# {{form(form)}}; #}
    {# ! la fonction form() affiche tout le formulaire d'un seul block non modifiable #}

    {{form_start(form)}}

    {# ! en twig un tableau { indice: valeur } en PHP [ indice => valeur ] #}

        {{form_row(form.title , {

        label: "Title",
        attr: { placeholder: "le titre de l'article"}

        }) }}

        {{form_row(form.image)}}
        {{form_row(form.content)}}

        <button type="submit" class="btn btn-primary">Envoyer</button>
    {{form_end(form)}}

{% endblock %}

